apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wordpress
  namespace: wordpress
spec:
  rules:
    - host: wordpress.192.168.0.200.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              serviceName: wordpress
              servicePort: 80