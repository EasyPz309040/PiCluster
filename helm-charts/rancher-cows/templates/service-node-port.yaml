apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-cows.fullname" . }}-nodeport
  labels:
    {{- include "rancher-cows.labels" . | nindent 4 }}
spec:
  type: {{ .Values.serviceNodePort.type }}
  ports:
    - port: {{ .Values.serviceNodePort.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher-cows.selectorLabels" . | nindent 4 }}
