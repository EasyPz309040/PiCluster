apiVersion: v1
kind: Service
metadata:
  name: {{ include "rancher-cows.fullname" . }}-nodeport
  labels:
    {{- include "rancher-cows.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service-np.type }}
  ports:
    - port: {{ .Values.service-np.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rancher-cows.selectorLabels" . | nindent 4 }}
